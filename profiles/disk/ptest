#
# disk/ptest
#

local state=$(print /sys/class/power_supply/A*/online(.N))

if test -n $state -a -r $state; then
	state=$(< $state)
	case $state in
		1) echo "adp";;
		0) echo "bat";;
	esac
fi

#
# vim:fenc=utf-8:ft=zsh:ci:pi:sts=0:sw=4:ts=4:
#
